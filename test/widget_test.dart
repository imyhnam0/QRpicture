import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Firebase 초기화가 필요해 기본 smoke test는 제외
    expect(true, isTrue);
  });
}
